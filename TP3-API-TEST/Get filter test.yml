info:
  name: Get filter test
  type: http

http:
  method: GET
  url: http://localhost:3000/api/tasks/completed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

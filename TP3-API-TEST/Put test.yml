info:
  name: Put test
  type: http

http:
  method: PUT
  url: http://localhost:3000/api/tasks/0
  body:
    type: json
    data: |-
      {
        "id": 0,
        "complété": true,
        "titre": "mido"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Post test
  type: http

http:
  method: POST
  url: http://localhost:3000/api/tasks
  body:
    type: json
    data: |-
      {
        "id": 6,
        "complété": true,
        "titre": "mid"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
